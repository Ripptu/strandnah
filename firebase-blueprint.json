{
  "entities": {
    "Listing": {
      "title": "Listing",
      "description": "A property listing for rent or sale",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "location": { "type": "string" },
        "price": { "type": "string" },
        "rating": { "type": "number" },
        "reviews": { "type": "number" },
        "images": { 
          "type": "array",
          "items": { "type": "string" }
        },
        "features": { 
          "type": "array",
          "items": { "type": "string" }
        },
        "amenities": {
          "type": "array",
          "items": { "type": "string" }
        },
        "areaImages": {
          "type": "object",
          "properties": {
            "livingRoom": { "type": "string" },
            "kitchen": { "type": "string" },
            "dining": { "type": "string" },
            "bedroom1": { "type": "string" },
            "bedroom2": { "type": "string" },
            "bedroom3": { "type": "string" },
            "bathroom": { "type": "string" },
            "guestWc": { "type": "string" },
            "outdoor": { "type": "string" }
          }
        },
        "pdfLinks": {
          "type": "array",
          "items": { "type": "string" }
        },
        "description": { "type": "string" },
        "type": { "type": "string", "enum": ["rental", "sale"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "location", "price", "images", "type"]
    },
    "Booking": {
      "title": "Booking",
      "description": "A reservation for a property",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "listingId": { "type": "string" },
        "startDate": { "type": "string", "format": "date-time" },
        "endDate": { "type": "string", "format": "date-time" },
        "guests": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "confirmed", "cancelled"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["listingId", "startDate", "endDate"]
    }
  },
  "firestore": {
    "listings": {
      "schema": "Listing",
      "description": "Collection of all property listings"
    },
    "bookings": {
      "schema": "Booking",
      "description": "Collection of all property reservations"
    }
  }
}
